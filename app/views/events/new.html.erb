 
<h1>Create a new event!</h1>
<%= form_for @event do |f| %>
  <%= f.label :description %>
  <%= f.text_area :description %>

  <%= f.label :date %>
  <%= f.date_field :date %>

  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>
